/*import { Meteor } from 'meteor/meteor';*/
import Ads from './ads';

/*
Meteor.methods({
    'Ads.insert': function (message, imageId, imageURL) {
        const post = {
            user: Meteor.user(),
            createdAt: new Date(),
            image: imageId,
            imageurl: imageURL,
            message,
            likes: [],
            comments: [],
        };

        Ads.insert(post);
    },
});*/
